version: '3'

services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"   # 3001 (خارج) → 3000 (داخل کانتینر)
    environment:
      - NODE_ENV=production